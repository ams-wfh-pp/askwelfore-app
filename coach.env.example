# DOCUMENTATION ONLY — never commit real credentials.
# Nothing is activated by this file; there is no automatic .env loading.
COACH_ENABLED=0
COACH_AI_ENABLED=0
COACH_ACCESS_CODE=
COACH_ORIGIN=
COACH_OPENAI_API_KEY=
COACH_MODEL=
COACH_REASONING_EFFORT=
COACH_MAX_OUTPUT_TOKENS=700
# COACH_LOCAL_HTTP=1 is permitted only for loopback development.
